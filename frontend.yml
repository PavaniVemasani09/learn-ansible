- name: "Frontend expense setup"
  hosts: all
  become: yes
  tasks:
    - name: "Install nginx"
      ansible.builtin.dnf:
        state: latest

    - name: "Start nginx"
      service:
        name: "nginx"
          state: started
          enabled: yes