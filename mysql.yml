- name: mysql-server expense setup
  hosts: all
  become: yes
  tasks:
    - name: Install mysql-server
      ansible.builtin.dnf:
        name: mysql-server
        state: latest

    - name: Start mysql server service
      ansible.builtin.systemd_service:
        state: started
        name: mysqld